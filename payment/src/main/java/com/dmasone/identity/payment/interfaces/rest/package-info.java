/**
 * Payment HTTP API.
 *
 * <p>This package exposes a small read endpoint for payment attempts so demos
 * and tests can verify that order events were handled. It should remain a thin
 * translation layer over payment application services.</p>
 */
package com.dmasone.identity.payment.interfaces.rest;
