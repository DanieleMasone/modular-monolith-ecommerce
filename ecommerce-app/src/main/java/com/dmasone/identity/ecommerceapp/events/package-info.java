/**
 * Spring event adapters for domain events.
 *
 * <p>This package bridges the shared event publisher port to Spring's in-process
 * event mechanism. It keeps event transport local to the modular monolith and
 * avoids introducing external brokers before they are justified.</p>
 */
package com.dmasone.identity.ecommerceapp.events;
